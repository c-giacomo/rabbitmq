spring:
    application:
        name: rabbit-app
    rabbitmq:
        host: localhost
        port: 5672
        username: guest
        password: guest
    amqp:
        deserialization:
            trust:
                packages: com.rabbitmq.model
